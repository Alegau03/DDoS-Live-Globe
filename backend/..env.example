# Attiva UNA sorgente (mock o Radar o Cloudflare per zona)

# MOCK
USE_MOCK_INGEST=true
USE_RADAR_INGEST=false

# RADAR (reale, non serve dominio)
# USE_MOCK_INGEST=false
# USE_RADAR_INGEST=true
# RADAR_API_TOKEN=cf_radar_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
# RADAR_DATE_RANGE=1d
# RADAR_LIMIT=20

# CLOUDFLARE PER-ZONA (richiede dominio)
# USE_MOCK_INGEST=false
# USE_RADAR_INGEST=false
# CLOUDFLARE_API_TOKEN=cf_...
# CLOUDFLARE_ZONE_TAG=...

# AbuseIPDB (opzionale)
ABUSEIPDB_KEY=
ABUSEIPDB_TTL_SEC=3600

# Privacy (per hash IP quando presenti)
HASH_IP_SALT=change_me_to_a_long_random_string

